# AI Content Engine v2 — environment variables
# Copy into your n8n instance (Settings -> Variables, or host .env named exactly ".env").
# NEVER commit real values. All secrets live here / in n8n Credentials — never in the workflow JSON.

# --- Control ---
KILL_SWITCH=false                 # set true to short-circuit every run instantly
TOKEN_BUDGET=200000               # max Claude tokens per run (cost cap guard)
CLAUDE_MODEL=claude-sonnet-4-5    # provider model id

# --- Alerting / observability (Charles) ---
ALERT_WEBHOOK_URL=                # Slack/Email webhook for failure alerts
SLACK_DIGEST_URL=                 # Slack webhook for the success digest

# --- External endpoints (client systems) ---
ANALYTICS_ENDPOINT=               # GA4 / YouTube / GHL analytics aggregator
RESEARCH_ENDPOINT=                # SEO + trend + solar-incentive research API
APPROVAL_ENDPOINT=                # GHL/Slack human-approval webhook (strategy + editor)
IMAGEGEN_ENDPOINT=                # image generation (Pixa/DALL-E/etc.)
ZOHO_UPLOAD_ENDPOINT=             # Zoho WorkDrive upload
CLIPPER_ENDPOINT=                 # video clip auto-cutter (Vizard/Opus/etc.) — SWAP POINT
YOUTUBE_ENDPOINT=
WORDPRESS_ENDPOINT=
LINKEDIN_ENDPOINT=
FACEBOOK_ENDPOINT=
INSTAGRAM_ENDPOINT=
EMAIL_ENDPOINT=                   # GHL / Klaviyo newsletter send
GHL_LEADS_ENDPOINT=               # GHL lead capture / tagging

# --- Credentials (create in n8n -> Credentials, type "Header Auth"; map by name) ---
# Workflow references credential placeholders REPLACE_<NAME>. In n8n UI, open each
# HTTP node and select the matching credential. Header names below.
#
#   ANTHROPIC   -> header: x-api-key       value: <Anthropic API key>
#   GHL         -> header: Authorization   value: Bearer <GHL token>
#   ANALYTICS   -> per provider (GA4/YT OAuth)
#   RESEARCH    -> header: Authorization   value: Bearer <SEO API key>
#   IMAGEGEN    -> header: Authorization   value: Bearer <image-gen key>
#   ZOHO        -> header: Authorization   value: Zoho-oauthtoken <token>
#   CLIPPER     -> header: Authorization   value: Bearer <clipper key>
#   YOUTUBE     -> OAuth2 (Google)
#   WORDPRESS   -> header: Authorization   value: Basic <app-password base64>
#   LINKEDIN    -> OAuth2 (LinkedIn)
#   FACEBOOK    -> OAuth2 (Meta Graph)
#   INSTAGRAM   -> OAuth2 (Meta Graph)
#   EMAIL       -> header: Authorization   value: Bearer <GHL/Klaviyo key>
